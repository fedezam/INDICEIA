import { defineConfig } from 'vite'

export default defineConfig({
  // acá podés dejar vacío o poner lo que quieras después
  // por ahora lo dejamos limpio porque ya no lo necesitamos para el build
})
