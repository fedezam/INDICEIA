// Resolver de horario cognitivo: devuelve true si la IA puede activarse en este momento
export async function resolveHorarioCognitivo(comercio_id) {
  // Placeholder: siempre activo
  return true;
}
